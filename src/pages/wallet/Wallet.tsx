import {
  View,
  ImageBackground,
  StatusBar,
} from 'react-native';

const Wallet = ({ navigation }: any) => {
    return (
        <View style={{ flex: 1, marginTop: 20 }}></View>
    )
}

export default Wallet;
